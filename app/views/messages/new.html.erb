<div class="container">
<div class="messages-header">
  <h1 class="messages-header text-center presentation-line"> New message to the Host</h1>
    <%= link_to "All messages", reservation_messages_path(@reservation), class: "btn medium-btn", style: "background-color: #1fdc89" %>
  </div>
</div>
  <div class="wrapper">
    <div class="container">
    <br><br>
      <%= simple_form_for [@reservation, @message] do |f| %>
      <%= f.input :content %>
      <%= f.submit "Add a message", class: 'btn btn-flat-card-book' %>
      <% end %>
    </div>
  </div>

